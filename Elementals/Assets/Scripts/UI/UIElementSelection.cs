using UnityEngine;

namespace UI
{
    public class UIElementSelection : MonoBehaviour
    {
        
        /// <summary>
        /// basically this toggles between a debug state where all elements are available and the normal gameplay state where the only the
        /// elements that have been chosen by the player are able to be selected 
        /// </summary>
        /// <param name="enabled"></param>
        public void SetAllElementsEnabled(bool enabled)
        {
            
        }
    }
}